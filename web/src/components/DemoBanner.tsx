export default function DemoBanner() {
  return (
    <div style={{
      background: '#e65100',
      color: '#fff',
      padding: '8px 24px',
      textAlign: 'center',
      fontSize: 13,
      fontWeight: 600,
    }}>
      Demo Mode — Showing sample data. Read-only preview.
    </div>
  );
}
