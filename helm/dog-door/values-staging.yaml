# Staging environment overrides

api:
  replicas: 1

web:
  replicas: 1

ingress:
  enabled: true
  host: dogdoor-staging.example.com

config:
  aspnetcoreEnvironment: Staging

secret:
  jwtSecretKey: "staging-override-set-real-secret-via-helm-set-in-deploy"
